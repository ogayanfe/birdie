import Card from "../global/Card";
import React from "react";

const Home = () => {
    return (
        <div className="flex flex-col items-center w-full">
            <Card />
        </div>
    );
};

export default Home;
